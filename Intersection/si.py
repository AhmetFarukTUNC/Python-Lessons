setA = set((1,2,3,4,5)) # We defined a set and assigned to setA.

setB = set((1,3,4,6,7,8)) # We defined a set and assigned to setB.

print(setA & setB) # We did intersection operation for setA and setB and printed this operation result.

print(setA.intersection(setB)) # We did intersection operation for setA and setB and printed this operation result.