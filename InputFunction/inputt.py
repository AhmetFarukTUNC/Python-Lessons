x = input("What Is The Number? : ") # We took input and assigned value of input to x.

y = input("What Is The Number? : ") # We took input and assigned vakue of input to x.

z = float(x) + float(y) # We converted x and y to float and assigned result to z.

print("Result : " + str(z)) # We printed the stringified version of the result.

diameter = input("Input value of diameter : ") # We took diameter from user.

radius = float(diameter) / 2 # We found radius with divide to two.

pi = 3.14159 # We defined pi as const

area = pi * radius * radius # We found area of circle.

print("Area of Circle is " + str(area)) # We printed area of circle.

