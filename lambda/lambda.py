calculateRectangularArea = lambda a,b:a*b/2 # We defined side of triangle as a and b.

print(calculateRectangularArea(3,6)) # We determined 3 and 6 side of triangle and printed.

print(type(calculateRectangularArea)) # We printed type of calculateReactangularArea function.

x = calculateRectangularArea # We assigned calculateRectangularArea function to x.

print(x(4,5)) # We determined side of triangle as 4 and 5 for a and b and printed return value of function to screen.